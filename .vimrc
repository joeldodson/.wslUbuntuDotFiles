" settings to disable anything causing a screen reader to interrupt 
set laststatus=0
set noruler
set noshowmode
set noshowcmd

set tabstop=4                     "Indentation levels every four columns
set expandtab                     "Convert all tabs typed to spaces
set shiftwidth=4                  "Indent/outdent by four columns
set softtabstop=4
filetype indent plugin on " indentation rules specific to file type 

set title " change window title to file being edited 

" Use case insensitive search, except when using capital letters
set ignorecase
set smartcase


